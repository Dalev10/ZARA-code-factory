/**
 * Puertos de salida que el dominio del módulo catalogo requiere de la infraestructura (persistencia, servicios externos).
 *
 * Módulo: catalogo — Categoría, Template y Variante (SKU) — catálogo de producto
 */
package com.codefactory.supplychain.catalogo.application.port.out;
